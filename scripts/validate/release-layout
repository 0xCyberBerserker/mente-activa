#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$repo_root"

scripts/release/build-release-metadata "${1:-$repo_root/dist}" >/dev/null
printf 'release-layout-ok\n'
